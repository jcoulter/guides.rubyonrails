<%= form_with(model: [ @article, @article.commetns.build ]) do |form| %>
	<p>
		<%= form.label :commenter %><br>
		<%= form.text_field :commetner %>
	</p>
	<p>
		<%= form.label :body %><br>
		<%= form.text_area :body %>
	</p>
	<p>
		<%= form.submit %>
	</p>
<% end %>
